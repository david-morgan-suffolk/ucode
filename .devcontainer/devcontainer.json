{
  "name": "ucode",
  "image": "mcr.microsoft.com/devcontainers/python:1-3.12",
  "postCreateCommand": "pipx install uv && (test -f pyproject.toml && uv sync || echo 'no pyproject.toml yet')",
  "customizations": {
    "vscode": {
      "extensions": ["ms-python.python", "charliermarsh.ruff"]
    }
  },
  "remoteUser": "vscode"
}
